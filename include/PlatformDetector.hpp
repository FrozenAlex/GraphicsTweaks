#include <string>

namespace GraphicsTweaks {
    // Is it a quest 1? (needs to be set in setup)
    extern bool isQuest1;
    extern std::string model;
    extern std::string brand;
    

    void DetectPlatform();
}